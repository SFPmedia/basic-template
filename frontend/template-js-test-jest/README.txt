For installation:

In terminal:
    1. Run "npm init"
    2. Run "npm i -D jest"
    3. Go to the "package.json" file and make sure that the value of "test" under "scripts" is "jest".
    4. Run "npm i axios"

Then you should be good to go.